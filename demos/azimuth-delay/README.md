# Shader Compilation

Before you can run the azimuth-delay diagram demo, you must compile the fragment and vertex shaders that are used to display the received power overlay.
For this, just run the `compile_shader.sh` script.